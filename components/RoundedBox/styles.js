import {StyleSheet} from 'react-native';
import {wp, hp} from '../../dimensions/dimensions';

export const styles = StyleSheet.create({
  root: {
    borderWidth: 4,
    borderColor: '#E1F1FF',
    borderRadius: 50,
    margin: 5,
    marginLeft: 25,
    marginRight: 25,
    justifyContent: 'center',
    flex : 1,
    
  },
});

export default styles;
